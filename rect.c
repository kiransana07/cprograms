#include<stdio.h>
float main()
{
float w,h,a;
printf("key is the first value");
scanf("%f", &w);
printf("key is the second value");
scanf("%f", &h);
a=w*h;
printf("the input values of w and h %.2f k%.2f\n",w,h);
printf("the area of rectangle is a=%.2f\n",a);
}
